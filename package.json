{
  "name": "atelier-breande-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npm run gardes",
    "build:nu": "astro build",
    "preview": "astro preview --port 4341",
    "check": "astro check",
    "gardes": "node scripts/verifier-livrable.mjs && node scripts/verifier-poids.mjs && node scripts/verifier-typographie.mjs && node scripts/verifier-fiction.mjs",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "verifier-notes": "node scripts/verifier-notes-lighthouse.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/archivo": "^5.3.0",
    "@fontsource-variable/fraunces": "^5.3.0",
    "@fontsource-variable/instrument-sans": "^5.3.0",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.1.1",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.62.0",
    "lighthouse": "^13.4.1",
    "playwright": "^1.62.0",
    "typescript": "^5.9.0"
  },
  "description": "Site vitrine de demonstration - Atelier Breande, atelier de luminaires fictif (Lyon).",
  "engines": {
    "node": ">=22.12.0"
  }
}
